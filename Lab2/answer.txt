Answer:

When we create a constructor method to build a DVD by title then build by category, Java notify us that there is a duplication. So it's an error. But it's different when testing in the Aims.java file. We can run even get warning.